# ADR 019: Staff locale and storefront locales are separate

**Status:** accepted

Each backoffice user can choose a product language independently. Each restaurant tenant separately chooses which storefront locales are published and which locale is primary.

A manager using Spanish can therefore manage a restaurant whose public site is published in Portuguese and English without coupling the two preferences.
