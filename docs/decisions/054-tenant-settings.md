# ADR 054: Tenant operational settings are data, not deployment configuration

**Status:** accepted

Restaurant timezone, currency, enabled locales, theme selection and module entitlements are tenant data. They are not separate environment variables or deployments per restaurant.
