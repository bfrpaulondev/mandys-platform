# ADR 052: V0.1 locale set

**Status:** accepted

The initial Mandy's locale set is `pt-PT`, `pt-BR`, `en` and `es`. `pt-PT` is the default product locale. Additional locales are data/config additions rather than schema redesigns.
