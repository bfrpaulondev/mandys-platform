# ADR 047: Theme customizations use controlled tokens

**Status:** accepted

Tenant theme customization starts with controlled design tokens such as color, radius and typography choices rather than arbitrary executable CSS or JavaScript. This keeps themes safe, portable and compatible with future visual upgrades.
