# ADR 063: Theme customization is data, not arbitrary code

**Status:** accepted

Tenant theme settings may customize approved tokens and layout options but do not accept arbitrary tenant JavaScript. Theme packages are reviewed application code and remain separate from tenant-provided content.
