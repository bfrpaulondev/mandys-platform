# ADR 056: Storefront owns public discovery concerns

**Status:** accepted

The public storefront is responsible for locale-aware metadata, canonical URLs, structured restaurant information and future search-engine indexing. The backoffice is an authenticated application and is not designed as a public SEO surface.
